# Leftovers — real, inert debris

Scope: what runs inside `run_cycle()`, `BytesFromToby/Polis` @ `04ec8f4`. Reach for any of
these and you get something real. It is simply no longer consumed.

| Artifact | Why it survives | What is live instead | Evidence |
|---|---|---|---|
| `world.initiative_order` | Written every pass at `cycle/resolution.py:67` with the correct, current turn order. The serializer carries two comments explaining that it is cycle-only and not persisted. | The order is used locally in the loop at `resolution.py:70` and then discarded. Nothing outside that loop needs it. | Search scope: `**/*.py`, `*.vue`, `*.js` for `initiative_order`. Four hits: `models.py:138` (declaration), `resolution.py:67` (the write), `serializer.py:7` and `:164` (comments only). No reader. |
| `apply_domain_jealousy` | A complete, working function in `cycle/end_of_cycle.py:241-264` that docks -3 reputation from factions sharing a domain with an exempt one. Call it and it works. | Nothing in the pass calls it. Exemptions are granted at position 6, ticked at 19, cleared at 12; no jealousy is applied at any position. | Search scope: `BytesFromToby/Polis\**` for `apply_domain_jealousy`. Two hits: the definition, and a 2026-06-08 inspect report noting it "intact". No call site in `backend/`. |
| The runner's own step numbering | The docstring at `cycle/runner.py:5-8` promises "Steps 0–4". The body's section comments read Step 0, Steps 1–2, Steps 4–6, Item 5a, Item 5b, "Step 4–6 already ran above", Step 8, Step 9. There is no step 3 and no step 7; the docstring places project ticks at step 3 and they run after the step 4–6 block. | The execution order in `catalog.md`. Read the line numbers, not the labels. | Direct read of `cycle/runner.py:5-8, 94, 136, 146, 154, 164, 174, 182, 234`. |
| `end_of_cycle.py:52-53` — "4.5 Leadership need tick" | A numbered section heading whose body is a comment saying "this block is informational only". | Leader status actually advances at position 10, `run_leadership_events`. | Direct read of `cycle/end_of_cycle.py:52-53`. Two comment lines, no code between them and the next block. |
| `process_moneylender`'s `removal_countdown` branch | `special/moneylender.py:59-73` implements a full removal-coalition countdown against a `List[int]` parameter, and a committed test exercises it (`tests/test_special_factions.py:122-133`). | The runner calls it at `runner.py:245` without that argument, so the parameter is always `None` and the branch never runs in a real pass. The live removal spiral is `Mayor.removal_countdown` at position 24. | Direct read of `cycle/runner.py:244-246` and `special/moneylender.py:17-24, 59-73`. |
| `backend/city_sim.db`, `backend/city_sim_Old.db` | The repo was `city_sim` before it was `Polis`; both names are live on disk. A reader searching `city_sim` finds real database files. | `backend/polis.db` and the root `polis.db`. Out of this cut — the pass takes plain objects and touches no database. | `ls` at the repo root: four `.db` files present. |
